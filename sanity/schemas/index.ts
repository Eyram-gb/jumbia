import { category } from "./category";
import { parentCategory } from "./parentCategory";
import { product } from "./product";

const schemas = [parentCategory, category, product];

export default schemas;
